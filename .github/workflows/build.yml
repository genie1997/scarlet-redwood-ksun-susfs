name: Build kernel + publish flashable

# Manual "Run workflow" button, plus any pushed tag like v6.0 / v6.0-ksun-susfs.
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

# contents: write lets the built-in GITHUB_TOKEN create/update Releases (no PAT).
permissions:
  contents: write

env:
  # Bump this to force a fresh toolchain download; otherwise reruns reuse the cache.
  TOOLCHAIN_TAG: "2026-07-15"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source (preserves the drivers/kernelsu symlink on Linux)
        uses: actions/checkout@v4

      - name: Compute release tag + zip name
        id: meta
        run: |
          if [ "${{ github.ref_type }}" = "tag" ]; then
            TAG="${{ github.ref_name }}"
          else
            # Tags can't repeat; keep it a short readable date plus the run number
            # so same-day re-runs stay unique. Title + asset name stay clean.
            TAG="$(date -u +%Y.%m.%d).${{ github.run_number }}"
          fi
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "zip=scarlet-ksun-susfs.zip" >> "$GITHUB_OUTPUT"
          echo "Release tag: $TAG   ->   scarlet-ksun-susfs.zip"

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y git bc bison flex libssl-dev libncurses-dev \
            ccache libelf-dev cpio zip unzip curl wget python3 aria2 zstd \
            device-tree-compiler

      - name: Cache Neutron Clang
        id: cache-toolchain
        uses: actions/cache@v4
        with:
          path: ~/toolchains/neutron-clang
          key: neutron-clang-${{ env.TOOLCHAIN_TAG }}-v2

      - name: Set up Neutron Clang (only on cache miss)
        if: steps.cache-toolchain.outputs.cache-hit != 'true'
        run: |
          mkdir -p ~/toolchains/neutron-clang
          cd ~/toolchains/neutron-clang
          curl -LSs "https://raw.githubusercontent.com/Neutron-Toolchains/antman/main/antman" -o antman
          chmod +x antman
          ./antman -S
          ./antman --patch=glibc

      - name: Verify / repair Neutron Clang (ld.lld must work)
        # Runs on BOTH cache-hit and cache-miss. A restored cache can be missing or
        # have a broken ld.lld (the "ld.lld: not found" vdso failure); if so, force a
        # fresh ./antman -S to re-download/extract a complete toolchain before building.
        run: |
          TC="$HOME/toolchains/neutron-clang"
          export PATH="$TC/bin:$PATH"
          need_fresh=0
          "$TC/bin/ld.lld" --version >/dev/null 2>&1 || { echo "ld.lld missing/broken"; need_fresh=1; }
          "$TC/bin/clang"  --version >/dev/null 2>&1 || { echo "clang missing/broken";  need_fresh=1; }
          if [ "$need_fresh" = 1 ]; then
            echo "::warning::Toolchain incomplete (bad cache restore) — re-syncing with antman"
            mkdir -p "$TC"; cd "$TC"
            if [ ! -x ./antman ]; then
              curl -LSs "https://raw.githubusercontent.com/Neutron-Toolchains/antman/main/antman" -o antman
              chmod +x antman
            fi
            ./antman -S
            ./antman --patch=glibc
          fi
          # Fail loudly if it is still broken after repair.
          echo "=== toolchain check ==="
          "$TC/bin/ld.lld" --version
          "$TC/bin/clang"  --version | head -1
          # Export the toolchain bin to PATH for ALL later steps and their sub-makes (vdso).
          echo "$TC/bin" >> "$GITHUB_PATH"

      - name: Build kernel (build.sh)
        run: |
          # build.sh adds ~/toolchains/neutron-clang/bin to PATH itself and derives
          # KERNEL_DIR from its own location, so it runs unmodified from the checkout.
          # (PATH is also set globally via $GITHUB_PATH in the verify step above.)
          export PATH="$HOME/toolchains/neutron-clang/bin:$PATH"
          chmod +x build.sh
          ./build.sh
          if [ ! -f out/arch/arm64/boot/Image ]; then
            echo "::error::out/arch/arm64/boot/Image was not produced — build failed"
            exit 1
          fi
          echo "Image OK: $(du -h out/arch/arm64/boot/Image | cut -f1)"

      - name: Package AnyKernel3 zip
        run: |
          git clone --depth=1 https://github.com/osm0sis/AnyKernel3 ak3
          rm -rf ak3/.git
          # redwood AnyKernel3 config: raw uncompressed Image, no ramdisk repack.
          cat > ak3/anykernel.sh <<'EOF'
          ### AnyKernel3 - Scarlet redwood (KSU-Next legacy + SuSFS v2.1.0)
          properties() { '
          kernel.string=Scarlet KSU-Next legacy + SuSFS v2.1.0 (genie1997)
          do.devicecheck=1
          do.modules=0
          do.systemless=1
          do.cleanup=1
          do.cleanuponabort=1
          device.name1=redwood
          device.name2=redwoodin
          device.name3=
          device.name4=
          device.name5=
          supported.versions=
          supported.patchlevels=
          '; } # end properties

          # shell variables — MUST be UPPERCASE: ak3-core.sh reads $BLOCK / $IS_SLOT_DEVICE
          # (no lowercase fallback; lowercase leaves $BLOCK empty -> "Invalid partition" abort)
          BLOCK=/dev/block/bootdevice/by-name/boot;
          IS_SLOT_DEVICE=1;
          RAMDISK_COMPRESSION=auto;
          PATCH_VBMETA_FLAG=auto;

          # import AnyKernel3 core
          . tools/ak3-core.sh;

          # flash raw uncompressed kernel Image only, keep the stock ramdisk (no repack)
          split_boot;
          flash_boot;
          EOF
          cp out/arch/arm64/boot/Image ak3/Image
          ( cd ak3 && zip -r9 "../${{ steps.meta.outputs.zip }}" * -x '.git*' )
          ls -la "${{ steps.meta.outputs.zip }}"

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.meta.outputs.zip }}
          path: ${{ steps.meta.outputs.zip }}

      - name: Publish Release (built-in GITHUB_TOKEN)
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.meta.outputs.tag }}
          name: Scarlet KSU-Next + SuSFS
          files: ${{ steps.meta.outputs.zip }}
          body: |
            Automated CI build of the Scarlet redwood kernel.

            - Scarlet v6.0 base (Atom-X-Devs, GPL-2.0)
            - KernelSU-Next v3.1.0-legacy-susfs (manual hooks)
            - SuSFS v2.1.0
            - UAPI-2 backport (v3.3.0 manager compatible)
            - SET_SEPOLICY inline backport
            - simple_lmk sysfs hide

            Flash `${{ steps.meta.outputs.zip }}` in recovery (AnyKernel3).
